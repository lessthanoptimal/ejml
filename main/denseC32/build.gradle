dependencies {
    compile project(':main:core')
    testCompile project(':main:dense32')
    testCompile project(':main:core').sourceSets.test.output
}

idea {
    module {
        name = "EJML Dense C32"
    }
}